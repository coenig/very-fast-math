These templates are (for now) intended to be pasted over "envmodel_config.tpl.json" in the father folder. 

TODO: Find a good way to encode different tasks directly within the "envmodel_config.tpl.json".
